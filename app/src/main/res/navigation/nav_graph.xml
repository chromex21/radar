<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/radar_fragment">

    <fragment
        android:id="@+id/radar_fragment"
        android:name="com.yourpackage.RadarFragment"
        android:label="Radar"
        tools:layout="@layout/fragment_radar" />

    <dialog
        android:id="@+id/invite_dialog"
        android:name="com.yourpackage.InviteDialogFragment"
        android:label="Invite" />

    <fragment
        android:id="@+id/chat_fragment"
        android:name="com.yourpackage.ChatFragment"
        android:label="Chat"
        tools:layout="@layout/fragment_chat" />

    <fragment
        android:id="@+id/settings_fragment"
        android:name="com.yourpackage.SettingsFragment"
        android:label="Settings"
        tools:layout="@layout/fragment_settings" />

</navigation>
